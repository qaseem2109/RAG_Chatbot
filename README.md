# RAG_Chatbot
A RAG (Retrieval-Augmented Generation) chatbot that ingests custom blog content, embeds it using sentence-transformers, indexes it with FAISS, and answers user queries via a GPT-2 LLM — all wired together with LangChain LCEL and served through a Streamlit UI.
